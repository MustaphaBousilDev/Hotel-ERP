services:
  reservations:
    build:
      context: .
      dockerfile: ./apps/reservations/Dockerfile
      target: development
    command: npm run start:dev reservations
    ports:
      - '5000:5000'
    volumes:
      - .:/usr/src/app
  # mongo:
  #   image: mongo